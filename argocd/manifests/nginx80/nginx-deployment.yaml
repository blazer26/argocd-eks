apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx:1.14.2
        ports:
        - containerPort: 80
---
# apiVersion: v1
# kind: Service
# metadata:
#   name: nginx-service
#   labels:
#     app: nginx-service
# spec:
#   selector:
#     app: nginx
#   ports:
#     - protocol: TCP
#       name: nginx-service
#       port: 80
#       targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  # labels:
  #   app.kubernetes.io/name: ingress-nginx-loadbalancer
  #   app.kubernetes.io/part-of: ingress-nginx
  name: nginx-svc-lb
  # namespace: nginx
spec:
  type: LoadBalancer
  selector:
    # app.kubernetes.io/component: controller
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
